# Development Environment Variables Template
# Copy this file to .env.dev and update with your actual values

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=real_estate_db

# JWT Configuration
# Generate a secure secret using: openssl rand -hex 32
JWT_SECRET=REPLACE_WITH_SECURE_32_BYTE_HEX_STRING

# Server Configuration
PORT=8080
GIN_MODE=debug

# Instructions:
# 1. Copy this file: cp .env.template .env.dev
# 2. Generate a secure JWT secret: openssl rand -hex 32
# 3. Replace JWT_SECRET value with the generated secret
# 4. Update database credentials if needed
